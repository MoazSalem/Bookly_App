import 'package:flutter/material.dart';

class AudioBooksViewBody extends StatelessWidget {
  const AudioBooksViewBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
