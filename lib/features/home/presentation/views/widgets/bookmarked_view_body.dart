import 'package:flutter/material.dart';

class BookmarkedViewBody extends StatelessWidget {
  const BookmarkedViewBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}